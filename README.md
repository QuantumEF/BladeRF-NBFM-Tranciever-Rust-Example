Very Much a WIP, code is a mess, the modulation index is screwy, but it does function after some testing.
The default modulation index related values are probably not right. I will fix it eventually.
